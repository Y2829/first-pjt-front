import type { FC } from "react";

import { StyledBox, Title } from "./styles";

const Header: FC = () => {
  return (
    <StyledBox>
      <Title>Whai.</Title>
    </StyledBox>
  );
};

export default Header;
